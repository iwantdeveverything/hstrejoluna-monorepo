# openspec/config.yaml
schema: spec-driven

context: |
  Tech stack: TypeScript, React 19, Next.js 16, Sanity Studio, Turborepo.
  Architecture: Monorepo with NPM workspaces. Portfolio and Landing apps connected to Sanity CMS.
  Testing: Vitest (unit), Testing Library (integration), Playwright (e2e) detected in apps/portfolio.
  Style: Clean Code, Type Safety (no any), DRY, KISS. camelCase (vars/fns), PascalCase (components).

strict_tdd: false

testing:
  detected: "2026-04-27"
  runner:
    available: true
    framework: vitest
    command: npm run test --workspace=apps/portfolio
  layers:
    unit:
      available: true
      tool: vitest
    integration:
      available: true
      tool: "@testing-library/react + jsdom"
    e2E:
      available: true
      tool: playwright
  coverage:
    available: false
    command: null
  quality_tools:
    linter:
      available: true
      command: npm run lint
    type_checker:
      available: true
      command: npm run typecheck
    formatter:
      available: true
      command: npm run format

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions (AGENTS.md)
    - Load relevant coding skills for the project stack (Next.js, React, Sanity)
  verify:
    - Run tests if test infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas (large removals)
